const { partOne, partTwo } = require('./day24');

const testData = `

`;

test('Part One', () => {
  expect(partOne(testData)).toBe();
});

test('Part Two', () => {
  expect(partTwo(testData)).toBe();
});
