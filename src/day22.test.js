const { partOne, partTwo } = require('./day22');

const testData = `

`;

test('Part One', () => {
  expect(partOne(testData)).toBe();
});

test('Part Two', () => {
  expect(partTwo(testData)).toBe();
});
