const { partOne, partTwo } = require('./day7');

const testData = `

`;

test('Part One', () => {
  expect(partOne(testData)).toBe();
});

test('Part Two', () => {
  expect(partTwo(testData)).toBe();
});
