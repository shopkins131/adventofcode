const { partOne, partTwo } = require('./day1');

const testData = `

`;

test('Part One', () => {
  expect(partOne(testData)).toBe();
});

test('Part Two', () => {
  expect(partTwo(testData)).toBe();
});
